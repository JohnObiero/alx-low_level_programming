#include <stdio.h>
/**
 *my entry point-main
 *uses put instead of printf to give the output
 *Return 0
*/

int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return(0);
}
